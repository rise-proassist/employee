# Vite dev server API proxy target
# Example: http://localhost:8000 or http://localhost:8080
VITE_API_PROXY_TARGET=http://localhost:8000

# Optional: direct endpoint path used by frontend lookup (defaults to /api/searchAddress/)
# VITE_ADDRESS_API_PATH=/api/searchAddress/
